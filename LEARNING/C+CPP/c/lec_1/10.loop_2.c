// Using for loop

#include <stdio.h>
int main(void)
{
   /*Here we use for loop to print hello world 3 times where we set the initial i value to 0  and the set the limit to 3 and then we tell i to increment the value until it's reached the threshold */

    for (int i=0; i<3; i++)

    /* here we have removed the curly braces since it is only a one line code but if it is more than that then it might throw up a error */

        printf("Hello World\n");

}

